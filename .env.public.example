# Public Supabase Configuration
# These are PUBLIC credentials - safe to commit to your repository
# The anon key only provides read access controlled by Row Level Security (RLS)

# Replace with your actual Supabase project URL
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Replace with your actual Supabase anon/public key (NOT the service key!)
# This key is safe to share and commit to your repository
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-anon-key-here

# IMPORTANT SECURITY NOTES:
# 1. Only use the anon/public key, never the service key
# 2. Ensure Row Level Security (RLS) is enabled on your tables
# 3. The anon key is designed to be public - your data is protected by RLS
# 4. Users can only read data through the search_zones RPC function